# Prompt Queue — 2026-07-16

Status per prompt: ⬜ belum jalan · 🟡 sedang jalan · ✅ selesai

---

## ✅ 1. [01] OTP expiry 10 → 5 menit
**Referensi:** `references/01-mailpit-otp.png`

```
In OtpService (app/Services/OtpService.php) and the OtpToken model, change the OTP expiry window from 10 minutes to 5 minutes. Find every hardcoded 10-minute reference (service logic, config, and any frontend countdown/label text like "berlaku 10 menit") and update to 5. Update assertions in tests/Unit/OtpServiceTest.php to match. Run `php artisan test --filter=OtpServiceTest` after the change and report the result.
```

---

## ✅ 2. [02] Autofocus kolom OTP pertama
**Referensi:** `references/02-login-otp.png`

```
In the OTP input component used by the login-otp page (resources/js/pages/auth/otp-login.tsx and its OTP-input subcomponent), make the first digit input auto-focus as soon as the page mounts (useRef + useEffect, or autoFocus). Keep existing behavior intact: auto-advance to next box on type, backspace-to-previous, and paste-to-fill-all if it currently exists. Do not change layout/styling.
```

---

## ✅ 3. [03] Kelola Kuota OPD → Kelola OPD
**Referensi:** `references/03-kelola-kuota-opd.png`

```
Expand "Kelola Kuota OPD" into a full "Kelola OPD" feature, mirroring the existing Kelola FAQ CRUD pattern (Verifikator\FaqController + its Inertia pages/table+modal structure), applied to the Opd model instead of Faq.

Requirements:
- List all OPDs: name, kuota_total, kuota_used, actions (edit, delete only if kuota_used = 0).
- "Tambah OPD" opens a create form: name + kuota_total (kuota_used defaults to 0).
- Edit form updates name + kuota_total, reusing the existing ownership-authorization logic from UpdateQuotaRequest.
- Preserve the current kuota-edit route/prop contract per HANDOFF-BACKEND.md, or explicitly flag if the prop path must change.
- Use the Boost MCP database-schema and search-docs tools before writing code, per AGENTS.md.
```

---

## ✅ 4. [07/06/05/11] Audit kelengkapan data dari form-pendaftaran
**Referensi:** `references/06-kotak-masuk-verifikator.png`, `references/07-tinjau-pengajuan.png`, `references/05-detail-opd.png`, `references/11-opd-perlu-keputusan-a.png`

```
Audit these four pages/controllers to confirm they display complete, accurate data sourced from the public form-pendaftaran submission (internship_applications table):
- 06 kotak-masuk-verifikator (Verifikator\DashboardController inbox list)
- 07 tinjau-pengajuan (Verifikator review/detail page)
- 05 detail-opd (Opd\DashboardController detail page)
- 11 opd-perlu-keputusan-a (Opd "Perlu Keputusan" decision page)

For each page: verify every field submitted in form-pendaftaran — including Jurusan (optional), Keahlian, and surat_pengantar_path/cv_path/portfolio_path if present — is passed as an Inertia prop and actually rendered, with no missing or stale values versus the DB record. Fix any prop gaps using Inertia::optional() where suitable, WITHOUT renaming existing prop paths (contract from HANDOFF-BACKEND.md must hold). Enforce the role rule: Admin Verifikator sees Jurusan/Keahlian but may only fill "Catatan Khusus Admin Verifikator" — never Divisi/Pembimbing Lapangan/Penanggung Jawab.
```

---

## ✅ 5. [08] Intervention Image + validasi ukuran file
**Referensi:** `references/08-form-pendaftaran.png`

```
On the public form-pendaftaran (welcome #daftar form → StoreApplicationRequest):
1. Ensure the pas foto upload is processed with the Intervention Image package (intervention/image) — resize/normalize/compress before saving to the private disk. Check composer.json first; add the package if missing.
2. Add file-size validation: surat_pengantar_path and cv_path max 2MB (2048 KB) each; portfolio_path max 10MB (10240 KB). Update StoreApplicationRequest rules AND the frontend file-input/dropzone component with matching client-side size checks and Indonesian error messages (e.g. "Ukuran file maksimal 2MB").
3. Keep all three documents optional ("jika ada") — only enforce size limits, not requiredness.

```

## ✅ 6. [NEW] Progressive Fibonacci lockout for OTP verification
**Referensi:** `references/01-mailpit-otp.png`, `references/02-login-otp.png`

```
Add a progressive Fibonacci-based lockout to the OTP login flow, extending the existing rate-limit pattern (app/Services/RateLimitService.php, FormRateLimit model, OtpService) rather than building a parallel system.

Behavior:
1. After 3 consecutive WRONG OTP input attempts (not send attempts — verify attempts), block further verify attempts and require the user to request a new OTP (resend).
2. Track a per-user cycle counter: every time this "3-wrong-then-resend" cycle completes, increment the counter (persist it — DB column or cache keyed by user/email, following the existing FormRateLimit pattern).
3. The cooldown required before a new OTP can be sent/verified follows the Fibonacci sequence in MINUTES, indexed by the cycle counter: cycle 1 = 1 min, cycle 2 = 1 min, cycle 3 = 2 min, cycle 4 = 3 min, cycle 5 = 5 min, cycle 6 = 8 min, cycle 7 = 13 min, etc. (standard fib(n)).
4. Reset the cycle counter to 0 when EITHER of these happens first:
   a. The user successfully logs in (verifies OTP correctly), OR
   b. 24 hours have elapsed since the last failed cycle with no new failed attempt.
   Implement (b) as a check at read-time (e.g. "if last_failed_cycle_at < now()-24h, treat counter as 0") rather than a scheduled job, unless a cron job already exists for similar cleanup — check TransitionApplicationStatuses / existing scheduled commands first for the right pattern.
5. Server-side enforcement is mandatory: block resend and verify requests during cooldown with a clear validation error (Indonesian), not just a disabled button in the UI.
6. Show a live countdown on the OTP page in Indonesian, e.g. "Coba lagi dalam 2 menit 15 detik".
7. Add unit tests covering: 3 wrong attempts trigger lockout; cooldown durations follow Fibonacci correctly across repeated cycles; successful login resets counter to 0; simulated 24h elapsed also resets counter to 0 (use Carbon::setTestNow or similar).

Note: this interacts with the existing OTP-expiry-5-minutes change (see prompt #1) — the cooldown lockout is separate from OTP expiry; don't conflate the two. Confirm both work together correctly (e.g. a fresh OTP sent after cooldown still expires in 5 minutes).